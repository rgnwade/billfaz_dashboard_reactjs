import PUBLIC_URL from './url'

export const MENU = {
  LOGIN: `${PUBLIC_URL}`,
  LOGIN_KEY: '',
  ORDER: `${PUBLIC_URL}/orders`,
  ORDER_KEY: 'orders',
  DEPOSIT: `${PUBLIC_URL}/deposit`,
  DEPOSIT_KEY: 'deposit',
  PRODUCT: `${PUBLIC_URL}/product`,
  PRODUCT_KEY: 'product',
  CLIENT: `${PUBLIC_URL}/client`,
  CLIENT_KEY: 'client',
}

export default MENU
